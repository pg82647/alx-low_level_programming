#include "main.h"
/**
 * add - Adds 2 integers and return the result
 * @a: One of the numbers
 * @b: Another number to produce the sum based on
 *
 * Return: The sum
 */
int add(int a, int b)
{
	return (a + b);
}
