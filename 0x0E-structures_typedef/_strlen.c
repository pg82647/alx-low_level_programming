/**
 * _strlen - calculates length of a string.
 * @str: The string whose length we are finding
 *
 * Return: The length of the string.
 */
int _strlen(char *str)
{
	int len = 0;

	for (; str[len]!='\0'; len++)
	{
	}

	return (len);
}
